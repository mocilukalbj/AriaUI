# AriaUI 边界清单

> 配套：LIBARIA2_ARCHITECTURE_PLAN.md（D01–D08，§4–§8）、LIBARIA2_TEST_PLAN.md（C/A/F/G/P/S/M）。
> 统一配置代码真值：`Services/Engine/IAriaEngine.cs` 中的 `EngineRuntimeConfig`（命令队列 256、批量上限 32 + 50ms 预算、采样 500ms、关键队列 128、关闭期限 5s、网关 64KiB/8连接/10req/s/1024条记录等常量完全一致并在启动时强校验）。
> 原则：fail-fast + 防御在边界。阶段 1 探针确认后锁定到最终运行配置。

## 0. 统一配置来源与历史分歧消解表 (Single Source of Truth)

为彻底消解阶段 0 推进过程中出现的初值分歧（如 32 vs 16、500ms vs 250ms、128 vs 512），统一以 `Services/Engine/IAriaEngine.cs` 中的 `EngineRuntimeConfig` 为代码与文档的唯一真值来源。分歧消解裁定与依据如下：

| 配置项 | 统一基线真值 | 废弃分歧值 | 依据与裁定原因 |
|---|---|---|---|
| **每轮命令批量上限** (`BatchCommandBudget`) | **32** (时间预算 50ms) | 16 | M01 基线实测真实 RPC 单命令往返延迟 P95 仅 0.18ms，32 项批处理在 6ms 内即可完成，留出充足 CPU 余量，兼顾高吞吐与 UI 响应性；16 过小会导致频繁出入调度循环。 |
| **进度/快照采样间隔** (`SnapshotInterval`) | **500ms** | 250ms | 上游官方 libaria2 示例与文档推荐采样间隔为 500ms；250ms 在高并发下载场景下易产生无谓的 UI 渲染开销与事件抖动。 |
| **关键事件通道容量** (`KeyEventQueueCapacity`) | **128** | 512 | 关键事件通道仅传递任务状态机跃迁事件（Start/Pause/Stop/Complete），正常运行期间处于低水位；128 槽位足以吸收极端突发事件，512 浪费紧凑无锁环形缓冲区内存。溢出时严格遵循 D05 升级为 fatal。 |
| **命令队列上限** (`CommandQueueCapacity`) | **256** | - | 有界队列背压，满载立即拒绝（D06 / C08）。 |
| **关闭期限** (`ShutdownTimeout`) | **5s** | - | 超时后未开始命令以 TimeoutException 快速失败（C04）。 |
| **网关单帧大小** (`GatewayMaxFrameSizeBytes`) | **64 KiB** | - | 对应 Native Messaging 规范与 G02 边界。 |
| **网关请求记录数 / 存留期** | **1024 条 / 10 分钟** | - | 终态去重与超时解耦缓存容量（G08–G09 / C12）。 |

## 1. 运行循环（owner 线程）

| 边界 | 初值 | 依据 / 测试 |
|---|---|---|
| 单进程单 session，全部 native 调用同一 owner 线程 | 硬约束 | 上游单 session 限制 / D04 / C02 |
| `keepRunning=true`，`useSignalHandler=false` | 硬约束 | 上游默认 false 空任务即返 0 / §4.1 |
| RUN_ONCE 等待按 ~1s 计，不靠忙轮询降延迟 | 假设待探针确认 | 上游文档 / §4.2 探针 1–4 |
| 每轮命令批量上限 | 32 个 + 50ms 时间预算，先到先停 | §4.2 / C08 |
| 进度/快照采样 | 500ms | 上游示例 500ms / F08 |
| `DownloadHandle` 下一次 run 前释放 | 硬约束 | 上游头文件 / A05 |

## 2. 有界队列与背压

| 边界 | 初值 | 依据 / 测试 |
|---|---|---|
| 命令队列上限，满载拒绝并说明是否接纳 | 256，不静默丢 | D06 / C08 / C07 |
| 关键状态通道独立有界，不被进度覆盖 | 128，溢出按 fatal | §5.3 / §6 / A05 |
| 进度通道按 GID 合并，只留最新值 | 槽位=任务数上限 | §6 / F08 |
| 关闭信号不占命令队列，原子关闭接纳入口 | 硬约束 | §4.3 / C04 |
| 超时只结束等待，不代表未执行，不自动重加 | 硬约束 | §6 / C07 |

## 3. C ABI 与缓冲区

| 边界 | 初值 | 依据 / 测试 |
|---|---|---|
| 结构体含 size+version，定宽整数，UTF-8 带长度复制 | 硬约束 | §5.1 / A01–A02 |
| 单个文本字段上限 | 8 KiB，超限明确失败 | §5.2 / A02 |
| 单次事件读取上限 | 128 条，容量不足不出队 | §5.2 / A04 |
| BUFFER_TOO_SMALL 正常协议，最多重试一次，不重放写副作用 | 硬约束 | §5.2 / A03 |
| 未预期 C++ 异常转 fatal，不穿 ABI；错误线程不碰 session | 硬约束 | §5.2 / A01、A05 |

## 4. 引擎状态与 fail-fast 分类

| 类别 | 行为 |
|---|---|
| 下载错误（HTTP 失败等） | 只记任务错误码，UI 更新，其他继续 / C06 |
| 命令错误（非法 GID/选项/状态） | 当次请求失败，不重试不假成功 / C06 |
| 满载 / 未就绪 / 请求过期 | 明确拒绝，说明是否接纳 / §5.3 |
| ABI 不匹配 / 内存不变量坏 / 未预期 native 异常 / 关键溢出 | Faulted：停接纳，完成未决，留根因，不自动重建 / D04、D05 / C06、A05 |
| native 卡死 | 不跨线程 destroy，watchdog 记录走终止策略 / §4.3 / C05 |
| 内存损坏类 | 终止进程，不强行清理 / §4.3 |

## 5. 网关（照抄 §8.3，可直接转断言）

单帧 64 KiB / 每连接 1 未决 / 最多 8 连接 / 每扩展 10/s（突发 10）/ 初始期限 10s / 请求记录 1024 条、终态留 10 分钟 / 去重键=实例 ID+扩展 ID+requestId。对应 G02–G04、G08–G09。

## 6. UI 订阅与快照

| 边界 | 初值 | 依据 / 测试 |
|---|---|---|
| 慢订阅者超限显式结束，可请求完整快照 | 滞后 >5s 或 >10 修订 | §6 / F08 |
| 快照带 host 实例 ID + 修订号，旧实例延迟更新丢弃 | 硬约束 | §6 / F08 |
| 列表不固定前 100，变更可识别、可重取一致视图 | 硬约束 | §3 F05 / F05 |
| 文件状态检查不在每轮刷新同步扫盘 | 硬约束 | F09 / U01 |

## 7. 持久化与关闭

| 边界 | 初值 | 依据 / 测试 |
|---|---|---|
| 周期保存间隔 | 30s（沿用现有）+ 关闭再存 | §7 / F06 |
| 状态文件缺失按首次启动；损坏/不可读写必须报错不偷盖 | 硬约束 | §7 / F07 |
| 运行中保存失败 → Faulted | 硬约束 | §7 / F07 |
| 关闭期限内处理已接纳命令，未开始的以关闭超时结束 | 5s（探针校准） | §4.3 / C04 |

## 8. 当前 RPC 最小对照修复（§10 允许范围）

- [x] `ProcessIncomingMessage` 遇 null/未知 id 只记日志不抛，接收循环永不死（对应本次轮询停摆根因）。
- [x] `PollLoopAsync` 按 tick 包 try/catch，单轮失败记日志进下一轮；重连不依赖轮询任务存活。
- [x] 回归：脏帧注入后轮询存活；瞬断后 3s 内重连恢复快照。
