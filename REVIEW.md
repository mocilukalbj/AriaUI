# AriaUI 项目审查报告

> **更新日期**：2026-09-04
> **审查范围**：Models、Services、ViewModels、Views、应用生命周期、构建与发布配置
> **支持范围**：Linux（Wayland / X11）与 Windows；macOS 暂不纳入支持、CI、验收或发布范围
> **当前结论**：本轮 #47–#60 的代码问题均已修复；Release、格式检查、`linux-x64` Native AOT 和临时运行验证通过。正式发布前仍建议补仓库内自动化测试并实际跑通 Windows 发布任务。

## 一、架构概览

项目采用 MVVM 与依赖注入，主调用链为：

`View → ViewModel → AriaTaskService → AriaWebSocketRpcClient → aria2 JSON-RPC`

`AriaProcessService` 管理本地 daemon，`SettingsService` 负责配置，Tracker 与文件系统操作保持独立。显式 View 映射和源生成 JSON 元数据兼容 Native AOT。

## 二、本轮问题结论（#47–#60）

| 编号 | 状态 | 精简结论 |
|:---:|:---:|---|
| #47 | ✅ 已修复 | Tracker 订阅逐跳校验、关闭代理并固定到已验证公网 IP；正文中的 Tracker 也限制数量、协议、格式和公网解析结果。 |
| #48 | ✅ 已修复 | 托管启动使用正向 1 秒、反向 3 秒的鉴权探针和 5 秒总截止，同时限制响应为 64 KiB、检查子进程存活并拒绝接管外部实例。 |
| #49 | ✅ 已修复 | 首次生成的随机 RPC 密钥立即私密落盘；配置只加载一次；WebSocket 通过错误 token 拒绝和正确 token 成功后才发布连接状态。 |
| #50 | ✅ 已修复 | 设置按“热更新 / 重连 / 重启 daemon”分类应用；每次连接重放限速与 Tracker 设置，应用失败不再提示成功。 |
| #51 | ✅ 已修复 | 每代 WebSocket 独立持有 socket、CTS、接收任务和 pending 集合；旧代不能覆盖新代状态，pending 可收到原始断线/协议异常。 |
| #52 | ✅ 已修复 | 托管配置仅继承非托管用户项；daemon/RPC、目录、并发、DHT、会话等选项由应用接管；Tracker 按准确 endpoint 保存并恢复原始值。 |
| #53 | ✅ 已修复 | Tracker 请求、响应和正文均确定性释放，取消贯穿正文读取，并设置 15 秒总时限与 512 KiB 上限。 |
| #54 | ✅ 已修复 | 先查询状态再选择 `remove` 或 `removeDownloadResult`；未知状态及 RPC 错误直接上抛，本地路径在远端变更前完成校验。 |
| #55 | ✅ 已修复 | 下载事件使用单消费者 dirty flag 合并刷新，不再为每个事件无限排队三次查询。 |
| #56 | ✅ 已修复 | 新建任务持有提交 CTS，取消从 ViewModel 贯穿批量添加、文件读取和 RPC 调用。 |
| #57 | ✅ 已修复 | 文件存在性只在完成状态或路径变化时异步检查，不再在每轮列表刷新中同步扫描。 |
| #58 | ✅ 已修复并实测 | 不再依赖 XAML 字符串 `ConverterParameter`；改为 ViewModel 派生布尔状态，真实控件切换结果正确。 |
| #59 | ✅ 已修复 | 空下载目录在加载及每次保存/字段更新时立即规范化为默认值；非空路径保持原值并在实际操作时暴露错误。 |
| #60 | ✅ 已修复 | 增加独立 `RpcUseTls`；外部 RPC 允许 `1-65535`，托管模式仅接受 `localhost`、`127.0.0.1`、`::1` 和 `1024-65535`，且拒绝 TLS。 |

## 三、额外稳固

- RPC 严格校验 JSON-RPC 版本、错误对象、`OK` 返回值和 16 位十六进制 GID，不再把畸形响应当成功。
- 轮询、重连、任务操作与设置切换通过同一连接锁串行化；后台异常会记录并保留到关闭阶段向上抛出。
- 关闭先取消生命周期，在连接锁内恢复外部 Tracker 或关闭托管 daemon 并断开 RPC，再等待轮询及已进入的添加、设置和 Tracker 操作；多项失败聚合上抛。
- 设置保存使用临时文件替换；Linux 上密钥配置在写入内容前设为 `0600`；字段更新在保存锁内完成，避免 Tracker 网络等待覆盖其他设置。
- 配置目录由 `ariaui.lock` 独占；PID 同时记录并核对进程号、启动时间和精确进程名，避免多实例或 PID 复用误杀。
- Tracker baseline 按协议、规范化主机和端口隔离；设置与实际连接 endpoint 不一致时直接失败，不向错误实例写配置。
- 文件打开、任务名解析和启动入口移除了业务层 blanket catch；不可恢复错误按原始异常向上暴露。

## 四、验证结果

| 检查 | 结果 | 说明 |
|---|:---:|---|
| `git diff --check` | ✅ | 无空白错误。 |
| Debug / Release `dotnet build --no-restore` | ✅ | 均为 0 编译错误；离线环境仅出现 NuGet 漏洞元数据 `NU1900` 警告。 |
| `dotnet format --verify-no-changes --no-restore` | ✅ | 格式检查通过。 |
| `linux-x64` Native AOT publish | ✅ | 发布到临时目录成功。 |
| 临时运行验证 | ✅ 8/8 套件 | 6 项综合验证、真实进程集成及空目录持久化回归全部通过。 |
| Windows 构建与运行 | ⏳ | 工作流已配置，本审查机未实际执行。 |

### #58 实测说明

在项目实际使用的 Avalonia 12.1.1 中，旧写法的 `int 0` 与 XAML 字符串 `"0"` 并未得到预期选中态，五个按钮初始均为 `false`。改用派生布尔属性后：

- 初始索引 0：`[true, false, false, false, false]`
- 执行第 4 个按钮命令：`[false, false, false, true, false]`
- 直接设置索引 1：`[false, true, false, false, false]`

因此 #58 已由真实 `TaskListView` 控件树确认，而不是仅凭静态推断。

### aria2c 集成实测

真实 aria2c 1.37.0 在约 2.8 秒内完成托管启动和正反鉴权；冲突用户配置被正确接管，密钥不出现在命令行且私有配置为 `0600`。第二实例、`127.0.0.2` 均在启动前失败；停止后 PID、私有配置和进程均无残留。

## 五、保留边界与技术债

- 多 URL 添加不是事务；若 aria2 已接受任务但响应、刷新或取消随后失败，重试可能重复添加。
- waiting/stopped 查询仍固定取前 100 项，大量历史任务需要分页或增量加载。
- Tracker baseline 仅保存在当前进程内；外部 daemon 跨应用重启或异常退出后，无法可靠辨别历史注入值。
- Tracker 地址只在订阅解析时校验 DNS；实际连接由 aria2 再次解析，AriaUI 无法固定最终目标 IP。
- WebSocket 无 pending 请求时的后台断线只体现为连接状态变化，根因日志仍可进一步增强。
- 仓库尚无正式测试项目；本轮验证位于临时 harness，不构成持续回归保护。
- Windows 发布矩阵尚需真实 CI 结果；Linux 发布包仍缺少 `.desktop` 桌面入口。
- 日志仍以 stderr 为主，尚未统一为结构化日志。

## 六、历史审查压缩摘要（#1–#46）

已确认且稳定的历史修复不再保留旧代码片段和过期行号：

| 范围 | 已落地内容 |
|---|---|
| #1–#18 | DI/MVVM 职责拆分、任务列表增量同步、基本输入校验、主题和异步生命周期基础。 |
| #19–#29 | RPC 发送互斥、pending 清理、接收大小限制、进程参数安全、文件删除根目录保护、Tracker 基础校验。 |
| #30–#38 | Linux/Windows 发布目标、AOT 显式映射、配置 Clone/原子保存、控件虚拟化和任务状态解析。 |
| #39–#46 | 托盘移除、关闭流程异步化、消息解绑、错误通知、URL 敏感 query 清理及平台路径整理。 |

仍有现实影响的内容已统一保留在第五节，不再重复早期“已修复过程”。

## 七、发布建议

本轮发现的高、中优先级代码阻断项已经关闭。建议把以下两项作为正式发布门槛：

1. 将当前临时验证迁入仓库测试项目，至少覆盖 RPC 代际/鉴权、配置迁移、Tracker SSRF 与 baseline、删除失败和 #58。
2. 实际跑通 Linux 与 Windows 发布矩阵，并做一次 Windows 启停、TLS 外部连接和文件打开冒烟测试。
