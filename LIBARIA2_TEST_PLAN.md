# libaria2 迁移测试与验收指导

> 更新：2026-09-05。配套设计见 [LIBARIA2_ARCHITECTURE_PLAN.md](LIBARIA2_ARCHITECTURE_PLAN.md)。
> 本文件是待实施的测试计划；以下用例均未因本文创建而视为通过。现有 REVIEW 的临时 harness 结果不能代替本计划的持续回归。

## 1. 测试目的与固定范围

测试验证架构决定 D01–D08 和用户可见结果，避免围绕实现写镜像测试或反复翻修已稳定模块。缺陷用最小复现固定，修复后执行受影响范围，通过即停止；不因“可能还有问题”无限扩测或重构。

| 本期必须覆盖 | 本期不覆盖 |
|---|---|
| Linux x64、固定发行版/glibc 基线、Debug/Release/Native AOT | Windows、macOS、ARM、所有发行版排列组合 |
| 单 session、本地 UI、实际 libaria2、受限浏览器接管 | 远程 daemon、多 session、HTTP/RPC 网关与端口降级 |
| 固定版本 Chromium 系浏览器与专用测试扩展 | 所有浏览器、商店审核、旧 RPC 扩展直接兼容 |
| GET/magnet、允许的 header、失败与未知结果 | POST、blob/data、DRM、网页运行环境复制 |
| 应用适配、生命周期、文件安全、资源边界 | 重测 aria2 全部协议、证明跨崩溃 exactly-once、保证零进度丢失 |

拒绝不支持输入、无控制端口和边界错误是本期测试；实现边界外功能不是修复要求。BT/DHT 数据端口不属于禁止的控制端口。

## 2. 环境、数据与证据

阶段 0 建立仓库内可重复运行的测试入口，按职责分组为契约、native 集成、网关协议、UI 冒烟与长时测试；无需为每组建立独立框架。实际入口和命令在首次实现时补入本节，未存在的命令不得写成已可运行。

- 测试使用独立临时配置、下载目录和私有运行目录；不能读取用户真实 Cookie、修改真实浏览器 profile 或删除真实任务。
- 契约测试用可控 fake engine 驱动取消、状态和接纳竞态；ABI 测试调用真实 bridge；网络/恢复测试调用真实 libaria2。fake 不能证明原生线程、资源释放和持久化正确。
- 集成源使用本地 HTTP/HTTPS 服务、测试 CA 与本地 BT peer/种子 fixture，内容与校验值固定；不依赖公网 Tracker、商业站点和实时下载速度。测试 CA 不改变产品 TLS 校验行为。
- Tracker SSRF 使用可控 DNS/重定向 fixture，按应用的公网校验规则验证拒绝，不能为让 fixture 通过而放松产品策略。
- 每次记录应用 commit、aria2 commit、依赖/工具链、构建模式、运行配置、OS/内核、CPU、浏览器版本及扩展 ID。性能对照额外记录负载、缓存状态和采样方法。
- 输出测试 ID、输入场景、实际/预期结果、耗时、诊断位置；失败保留脱敏日志、退出码和必要的 native 符号信息。HTTP fixture 中敏感标记用于检查日志泄漏，不使用真实凭据。
- 竞态用屏障/可控调度触发，不靠随机 sleep 猜测。跨进程等待使用截止时间与明确就绪条件。

每项结果只能为 Pass、Fail、Pending、Blocked 或有理由的 N/A。尚无实现为 Pending，缺运行环境为 Blocked；二者都不能算通过。适用的发布门禁不得用 N/A 跳过。

## 3. 稳定测试目录

测试 ID 一经建立不改名、不复用。实现可以拆分子案例，但不重复为同一行为建立多套断言。下列“通过条件”是观察结果，不限制私有类名或内部调用次数，除非调用次数本身决定安全性。

### 3.1 契约、生命周期与 ABI

| ID | 场景 | 通过条件 |
|---|---|---|
| C01 | 空任务启动、任务完成后再次添加 | 常驻 Ready，可继续添加；不把 run 自然结束伪装为就绪。 |
| C02 | 重复启动/关闭，各状态提交业务命令 | 结果符合状态契约，无重复 session；非 Ready 明确拒绝，重复关闭保留首次结果。 |
| C03 | Starting 中关闭，初始化各步骤失败 | 不再发布 Ready；只清理已创建资源，各请求结束，根因保留。 |
| C04 | 活动下载、满队列、等待写入时关闭 | 停止接纳，已接纳操作有明确结果；循环完成停机与保存，等待者不悬挂，线程退出。 |
| C05 | 关闭到期、native 卡住 | 非正常关闭可观测，不跨线程 destroy、不假成功；在独立测试子进程内验证终止策略，测试 runner 不随之挂死。 |
| C06 | 非法 GID/选项、下载错误、fatal 注入 | 前两类只影响请求/任务，后续有效命令可执行；fatal 完成未决请求并拒绝新命令，不自动重启。 |
| C07 | 取消与接纳/开始竞态、调用方超时 | 执行前取消无副作用；执行后真实结果可查询；超时不伪造取消、不自动重加，TCS 只完成一次。 |
| C08 | 持续命令压力、有限并发生产者 | native 得到推进，等待者/队列有上限；关闭信号不被满队列困住，continuation 不阻塞 owner。 |
| A01 | ABI 版本、尺寸、对齐、长度、错误线程 | 不匹配/坏输入在触碰 session 前失败；Debug/Release/AOT 对相同 DTO 解释一致。 |
| A02 | UTF-8、中文路径、空数组、零长度、超长字段 | 字节长度和边界正确，无截断、越界读取或无限分配。 |
| A03 | 添加/移除的结果容量不足，随后扩容 | 容量检查失败时写操作未执行；正确重试最多一次副作用；诊断读取不重放命令。 |
| A04 | 事件缓冲不足、两阶段读取 | 不提前出队、不丢关键事件；成功复制才消费，协议状态不误判 fatal。 |
| A05 | handle 各退出路径、C++ 异常、native 队列溢出 | handle 在下次 run 前释放，异常不跨 ABI；关键溢出明确 Faulted，内存诊断无新增问题。 |

### 3.2 核心功能、恢复与 UI

| ID | 场景 | 通过条件 |
|---|---|---|
| F01 | HTTP/HTTPS、torrent、magnet 添加到完成 | 内容校验正确；GID、元数据后续任务关联和错误状态可信；HTTPS 无不当跳过证书校验。 |
| F02 | 暂停/恢复、活动移除、历史清理、批量部分失败 | 不混淆“接受”与“完成”；按状态操作，逐项报告结果，失败项不假成功。 |
| F03 | 移除并删除文件、非法/链接/目录外路径 | 校验在副作用前；停止文件访问后再删；native 操作失败不继续删文件，保留既有路径保护。 |
| F04 | 全局/任务选项，保存成功但应用失败 | 实际值可读取；热更新/下次生效/重启分类准确，失败不显示全部成功。 |
| F05 | 0、1、101、1,000 项列表，翻页中变更 | 无固定前 100 项截断；变更可识别、分页可重新取得一致视图，历史和活动任务不会永久遗漏。 |
| F06 | 正常关闭、周期保存、强退后重启 | 任务清单与进度文件分别验证；恢复最后成功落盘内容，允许明确记录的崩溃窗口，不要求零损失。 |
| F07 | 缺失、损坏、只读状态文件和保存失败 | 缺失按首次启动；加载错误使启动失败，运行中保存失败使引擎 Faulted；不覆盖原文件、不悄悄空启动，保留可供用户备份的数据。 |
| F08 | 高频快照、慢订阅者、旧实例延迟更新 | owner 不被订阅者堵住；最新进度合并，关键流超限显式结束；旧实例数据不覆盖当前 UI。 |
| F09 | Tracker 校验/取消/释放、设置竞争、事件刷新 | REVIEW #47/#50/#53/#55 的相关保护保留；内核接入不绕开应用层校验。 |
| U01 | 真正 Avalonia 视图的筛选、错误提示、文件状态 | 保留 #58 选中态回归；失败有可见提示，文件检查不在每轮刷新同步扫盘；view 不依赖 RPC/native 类型。 |

补充回归 `Desktop/Paths`（对应 F01/U01，2026-09-06）：真实本地 HTTP 下载使用自定义中文文件名及超过初始 ABI 缓冲区的目录路径，核对 native 快照路径、大小、完成字节与实际文件一致；打开目录传父目录，文件/目录失效显示错误通知而不导致 UI 退出。该用例调用实际 ViewModel 命令，不替代真实桌面控件的视觉验收。

### 3.3 浏览器、Native Messaging 与 Unix Socket

| ID | 场景 | 通过条件 |
|---|---|---|
| G01 | manifest/扩展白名单、错误 origin/版本/操作 | 仅允许声明的入口；拒绝错误请求，无任意 RPC、shell、全局选项或删除能力。 |
| G02 | UTF-8 帧、分段读取、粘包、EOF、坏长度 | 按字节解析完整帧；半帧到期关闭，坏帧不执行；stdout 只有协议，不混应用启动日志。 |
| G03 | 64 KiB 边界、超限帧、未知字段、header 注入 | 分配前限长，未知/危险输入明确拒绝；CR/LF/NUL、路径穿越和非白名单选项不进入内核。 |
| G04 | 连接/速率/队列上限、慢读写 | 配置的限额与到期行为有效；接纳前拒绝，不创建无限等待者；坏连接不使 engine Faulted。 |
| G05 | 跨 UID、坏目录权限、符号链接、socket 替换 | 文件权限和双向 peer UID 检查有效，明确拒绝；同 UID 恶意进程隔离不作为承诺。跨 UID 真测需有相应 CI 环境。 |
| G06 | App 未启动、已运行、正在启动、并发冷启动 | 最终一个持锁 App/一个 session；已有锁不误判没启动，不删他人锁，不扫描端口，握手有期限。 |
| G07 | 陈旧 socket、权限错误、App 退出或 Faulted | 仅合法持锁 App 清理自己的旧 socket；宿主不杀进程、不循环拉起、不启动备用 HTTP 服务。 |
| G08 | 同 requestId 重发、载荷冲突、响应丢失 | 同实例只执行一次；重复返回 Pending/原结果，冲突失败；断开后查询原结果，不自动重加。 |
| G09 | 1,024 记录容量、终态过期、App 重启 | 不淘汰处理中记录；满载先拒绝；过期/旧实例报告 UnknownOutcome 或实例不匹配，不承诺跨崩溃去重。 |
| G10 | 真实浏览器 GET/magnet、Cookie/Referer/UA | 字段按允许语义送达本地 fixture；仅真实 GID 触发浏览器取消；原下载暂挂/恢复行为实测。 |
| G11 | 浏览器拒绝、超时未知结果、POST/blob/data | 不支持时保留原流程；明确拒绝可恢复；未知结果提示确认，不同时自动恢复与重加，不丢原任务信息。 |
| G12 | port 断开、App 单独关闭、宿主结束 | 宿主按连接生命周期退出，无孤儿队列/进程；宿主退出不停止已交给 App 的下载，脱敏日志不泄漏凭据。 |

协议单元测试不能替代 G10/G11 的真实浏览器验收。首次人工冒烟可作为阶段证据，正式候选必须有可重复步骤、fixture、版本与结果记录；后续自动化优先覆盖可稳定控制的流程。

### 3.4 打包、无控制端口与稳定性

| ID | 场景 | 通过条件 |
|---|---|---|
| P01 | 干净环境构建/发布 Debug、Release、AOT | 从锁定源码构建，可加载 bridge/依赖，不靠开发机 aria2；编译、仓库格式检查通过。 |
| P02 | 无外网、无预装 aria2 的目标基线环境启动 | UI/engine/宿主可启动；本地 fixture 可下载；无运行时补库，库路径和 TLS 信任来源明确。 |
| P03 | 启动、下载、插件接管及失败时检查控制链路 | 应用/宿主无 TCP/HTTP/WebSocket 控制监听、端口扫描、RPC secret 流程或 aria2c 子进程；仅指定 Unix Socket 提供控制 IPC。 |
| P04 | 依赖、许可证、源码/补丁/构建脚本、校验和 | 分发清单完整，从归档材料可重新构建功能等价产物；不要求未经设计的逐字节可复现。 |
| S01 | 1,000 次独立进程启停，含活动下载样本 | 无死锁、残留进程/线程与新增 native 泄漏。每次进程一个 session，不要求产品支持同 host 重启 1,000 次。 |
| S02 | 固定负载至少 24 小时，重复添加/结束/清理 | 无持续资源增长、关键事件静默丢失、未决请求遗留或下载饥饿；保留时序采样。 |
| S03 | ASan/UBSan native harness | 本次变更新增的问题为零；既有上游问题必须有可追溯记录，不能全局屏蔽。AOT 正常运行另测，不要求把所有工具硬塞进同一进程。 |
| M01 | 启动、命令、空闲 CPU、快照与内存对照 | 按第 4 节协议测量，超标按约定处理，不用机器噪声驱动反复改架构。 |

P03 使用进程/socket 观察与静态配置检查结合。无任务启动先隔离控制监听，再按已知 BT/DHT 配置区分数据 socket；必要时跟踪 bind/listen/connect。测试 fixture 自身的 HTTP/BT 端口不计为产品控制端口。仅检查“6800 没开”不足以通过。

## 4. 性能与稳定性测量边界

- 阶段 0 记录参考机器、OS、构建模式和 RPC 基线；阶段 1 固定 native 运行配置。相同数据源、任务数和构建模式下比较，不把 Debug 与 AOT 混比。
- 启动计时分别记录“进程启动到 engine Ready”和“进程启动到 UI 可交互”；空任务和恢复任务分组。每组至少 30 次，标明进程冷启动还是磁盘缓存冷启动，不混报。
- 500ms 是空任务 engine Ready 的试验目标；未达到只登记瓶颈和数据，不因此无限重构或阻止正确性阶段退出。
- 常用命令使用固定负载预热后各取至少 1,000 次，三轮记录 p50/p95/max；用三轮 p95 的中位数比较。候选门禁为不超过同机 RPC 基线的 110%（10% 为预先固定的噪声容差）。无可比基线时记 Blocked，不临时提高阈值。
- 启动测量与浏览器冷拉起分开，不能把宿主/浏览器耗时混入 engine 指标；添加延迟测到真实结果，不以入队时间冒充完成延迟。
- S02 预热 1 小时后每分钟记录 RSS、native 分配、线程/FD、队列水位、请求缓存与任务数；每轮结束清理历史并等待配置中的缓存期限。用固定数量存活任务比较相同阶段，避免把正常历史保留误判泄漏。
- 阶段 1 在运行配置中固定空闲 CPU、最大循环间隔、关闭期限、稳定态资源预算与增长容差，记录测量依据；这些字段未填写不能通过 M01/S02。候选失败后不得只改预算让测试变绿。
- S03 的确定泄漏/越界是硬失败，不以 RSS 尚未超限抵消。24 小时长测只在候选、native 升级或相关资源改动执行，不用于每次文案调整。

## 5. 阶段门禁与回归选择

| 阶段/变更 | 必须执行 | 停止规则 |
|---|---|---|
| 阶段 0 | 能力映射、既有共同契约与基线、测试入口/数据/预算登记 | 不要求未实现 native 用例通过；缺核心映射不迁 UI。 |
| 阶段 1 | C01–C08、A01–A05、F06/F07 的探针子集、S03、M01 参数测量 | 探针通过即进入能力迁移，不先做完整浏览器或 24 小时产品测试。 |
| 阶段 2 | 已实现能力对应 C/A/F/U 用例、共同 RPC 对照、P01 | 按新增能力扩大覆盖；不为未实现接口制造全套通过记录。 |
| 阶段 3 | G01–G12、P03，网关涉及的 C07/C08/F01 | 真实接管与故障路径通过才宣称支持插件。 |
| 阶段 4 候选 | 全部适用用例，P01–P04、S01–S03、M01 | 任一硬门禁 Fail/Pending/Blocked 均不删 RPC 回退。 |
| 阶段 5 删除旧后端 | P01–P03、U01、G06/G10/G12、C01/C04 及受删代码影响用例 | 仅删除隔离旧后端时不重复 24 小时测试；触及 native 生命周期则执行对应长测。 |

日常变更按以下最小范围执行，并补一个能复现缺陷的回归案例：

| 修改位置 | 回归范围 |
|---|---|
| 仅本指导/测试文档、注释 | Markdown/链接/编号/矛盾检查，不运行构建或长测。 |
| UI 展示、筛选、通知 | 相关 U01/F08/F05；编译/格式检查，不重跑 native 压测。 |
| 设置、Tracker、删除 | 相关 F03/F04/F07/F09；输入到副作用的真实边界，不能只测转换函数。 |
| Channel、取消、状态、关闭 | C01–C08、F06/F08；涉及 native 资源寿命再加 A05/S03/S02。 |
| bridge/ABI、aria2 或 native 依赖升级 | A01–A05、C01–C08、F01–F08、P01/P02、S03；候选执行 S01/S02/M01。 |
| 网关 schema/stdio/Unix Socket | 对应 G 组、G10/G11、P03；接纳/取消逻辑变化另加 C07/C08。 |
| 打包/工具链/安装 manifest | P01–P04、C01/C04、G01/G06/G10；不重复无关业务测试。 |

所有“相关”选择在变更记录中列出具体 ID。测试失败先定位根因；环境故障可在修复后复跑受影响组，不允许无改动不断重试直到偶然通过。已通过且无新改动/新证据，不重复扩大检查。

## 6. 既有审查的保留与退役

- REVIEW #47/#53 Tracker、#50/#52/#59 设置、#54 删除、#55 刷新、#56 取消、#57 文件检查和 #58 控件状态仍适用，分别关联 F09、F04/F07、F03、F08、C07、U01。
- #48/#49/#51/#60 中与 RPC 端口、secret、WebSocket 代际、外部 TLS endpoint 相关的测试，仅在 RPC 对照存在时保留；删除旧后端后标记退役及对应提交，不移植成原生需求。
- 浏览器权限和 Unix Socket 来源检查是新的 G 组边界，不能拿删除 RPC 鉴权测试作为已经覆盖的证据。
- 同一缺陷只保留最小而充分的回归，稳定历史不每轮重新全面审查。临时 harness 迁入仓库后指向正式用例，不并行维护两套权威结果。

## 7. 结果记录与修改规则

每个阶段/候选使用以下模板记录；实现时放入仓库测试结果目录，不在架构指导尾部堆叠历史评审。

```text
日期 / 应用 commit / aria2 commit / 构建模式：
OS / CPU / 浏览器及扩展 / 运行配置版本：
变更目标与涉及的 D 编号：
已执行测试 ID、状态、证据位置：
未执行 ID 与 Pending / Blocked 原因：
性能样本、阈值与比较结果（适用时）：
失败根因、最小修复与新增回归 ID：
已知边界或上游问题：
本阶段是否退出，下一步：
```

改变 D01–D08、支持范围、结果语义或硬阈值时，必须同一变更更新架构、对应测试和理由；普通参数调优带测量结果即可，不另设重复审批流程。不得删除失败断言、扩大容差或把未知结果当成功来完成验收。
